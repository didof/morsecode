import 'package:flutter/material.dart';

class ConverterActiveWithoutLampUI extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('not implemented'),
    );
  }
}
